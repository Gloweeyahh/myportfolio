// Animation logic (can use GSAP or another library here)
document.addEventListener('DOMContentLoaded', function() {
    // Example animation logic here
    const heroTitle = document.querySelector('.hero-section h1');
    heroTitle.classList.add('animated', 'fadeIn');
});
